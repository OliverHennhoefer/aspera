interface:
  display_name: "Setup"
  short_description: "Install and validate orchestrator role profiles"
  default_prompt: "Run $setup to install or check orchestrator profiles."
policy:
  allow_implicit_invocation: true
