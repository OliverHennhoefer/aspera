interface:
  display_name: "Setup"
  short_description: "Diagnose or remove an Aspera project installation"
  default_prompt: "Run $setup only to diagnose or remove an existing Aspera project installation."
policy:
  allow_implicit_invocation: true
