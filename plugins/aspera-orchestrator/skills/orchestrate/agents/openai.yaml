interface:
  display_name: "Orchestrate"
  short_description: "Route bounded implementation to Luna or conditional Spark"
  default_prompt: "Use $orchestrate for Luna-first implementation; Spark is conditional and availability failures immediately fall back to Luna."
policy:
  allow_implicit_invocation: false
