interface:
  display_name: "Orchestrate"
  short_description: "Route implementation through native Luna with minimal overhead"
  default_prompt: "Use $orchestrate for ultra-light native Luna-first implementation routing."
policy:
  allow_implicit_invocation: false
